@{
    ViewBag.Title = "Confirm Email";
}

<div class="card account-card shadow bg-body rounded">
    <h5 class="account-card-title">@ViewBag.Title</h5>
    <h6 class="account-card-subtitle">Thank you for confirming your email. Please @Html.ActionLink("Click here to Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</h6>
</div>
